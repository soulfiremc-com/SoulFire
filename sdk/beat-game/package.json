{
  "name": "@soulfiremc/beat-game",
  "version": "0.2.0",
  "description": "Effect-first beat-the-game runner for the official SoulFire SDK.",
  "keywords": [
    "minecraft",
    "bot",
    "soulfire",
    "effect",
    "automation"
  ],
  "author": "SoulFire",
  "license": "AGPL-3.0-only",
  "homepage": "https://soulfiremc.com/docs/sdk/automation",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/soulfiremc-com/SoulFire.git",
    "directory": "sdk/beat-game"
  },
  "bugs": {
    "url": "https://github.com/soulfiremc-com/SoulFire/issues"
  },
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./promise": {
      "types": "./dist/promise.d.ts",
      "import": "./dist/promise.js"
    },
    "./node": {
      "types": "./dist/node.d.ts",
      "import": "./dist/node.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "pnpm clean && tsc -p tsconfig.build.json",
    "check": "pnpm typecheck && pnpm test && pnpm build && pnpm typecheck:smoke && pnpm check:boundary && pnpm check:browser",
    "check:boundary": "node scripts/check-public-sdk-boundary.mjs",
    "check:browser": "node scripts/check-browser-bundle.mjs",
    "clean": "node scripts/clean-dist.mjs",
    "prepack": "pnpm build",
    "smoke:e2e": "pnpm --filter @soulfiremc/sdk build && pnpm build && pnpm typecheck:smoke && ../../gradlew -p ../.. :dedicated-launcher:uberJar && node --experimental-strip-types smoke/e2e.ts",
    "smoke:controlled": "SOULFIRE_E2E_CONTROLLED=true pnpm smoke:e2e",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "typecheck:smoke": "tsc -p tsconfig.smoke.json --noEmit"
  },
  "dependencies": {
    "@soulfiremc/sdk": "workspace:*"
  },
  "peerDependencies": {
    "effect": "^3.22.0"
  },
  "devDependencies": {
    "@bufbuild/protobuf": "2.14.0",
    "@effect/platform-node": "0.108.1",
    "@types/node": "24.13.3",
    "effect": "3.22.1",
    "esbuild": "0.28.2",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  },
  "publishConfig": {
    "access": "public"
  }
}
